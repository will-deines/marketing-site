# Imagen 3 Configuration File
# Source this file to set API parameters for image generation
# Based on latest 2025 Google Imagen 3 API specifications

# Required parameter
PROMPT="A professional product photography style image of an indie shop owner unboxing packages in a modern minimalist workspace, warm natural lighting from large windows, clean aesthetic with wooden desk and white walls, packages and shipping materials arranged artfully, shallow depth of field with soft background blur, shot with professional camera equipment, lifestyle photography style, high resolution, no text overlay"

# Core parameters (all officially supported as of 2025)
ASPECT_RATIO="16:9"                    # Options: "1:1", "3:4", "4:3", "9:16", "16:9" (default: "1:1")
SAMPLE_COUNT=1                         # Number of images to generate (1-4)
PERSON_GENERATION="allow_adult"        # Options: "dont_allow", "allow_adult" (default), "allow_all" (not available in EU/UK/CH/MENA)

# Note: Only core parameters are supported in the current API
# Parameters like negativePrompt, enhancePrompt, seed, outputOptions, etc. are not supported

# API endpoint configuration
MODEL_NAME="imagen-4.0-generate-preview-06-06"  # Latest Imagen model